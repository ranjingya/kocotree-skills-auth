services:
  kocotree-skills-auth:
    image: kocotree-skills-auth:2.0
    container_name: kocotree-skills-auth
    build:
      context: .
      dockerfile: Dockerfile
    env_file: 
      - .env
    ports:
      - "5050:5050"
    restart: on-failure
    networks:
      - kocotree-skills

networks:
  kocotree-skills:
    external: true
